Name: Anirudh Jutu
Task Description: Building a small artificial neural network and implementing backpropagation manually, using NumPy and Scikit-Learn.

Setup Instructions: 

1. Install a text editor such as Visual Studio Code, Jupyter Notebook, or Sublime Text. Even Python IDLE would work well.
2. Install NumPy and Scikit-Learn libraries using the pip command in command prompt/terminal.
3. Ensure you are not connected to a WiFi network requiring certification during the installation of libraries (especially matplotlib), as the certification blocks the installation process.

Commands to run the solutions:
1. In the text editor, debugging and running the file would run all the 6 programs/items.
